# app/test — the app vitest suite (23 files, 111 passed + 1 todo)

`npx vitest run` from `app/`. Families: p3 (the built-bundle secret gate +
the /analysis face), shell.face (menubar/tabs/save/dialog semantics through
the real App over a mocked hub), shell.units (pure chrome machinery),
shell1c (welcome/search, windows/accent, layout2 geometry/machine/persist,
and the cell-5 DOM skin guard), v4.serve (each file against its own spawned
real python hub) and v5.bus (the joined bus). Shared fixtures live in
`helpers/` (see `helpers/README.md`). Counts measured after this round's
doc fixes: 111 passed + 1 todo, `npx tsc --noEmit` exit 0
(`audit/AUDIT-app-tests.json`).

## Files (verified)

Verified purposes from `audit/AUDIT-app-tests.json`. Line counts measured on disk 2026-08-03 (post doc-fix state).

| file | lines | verified purpose |
|---|---|---|
| `p3.build.gate.test.ts` | 80 | Runs `npx vite build` and byte-scans every dist file (latin1 read) for five key-shaped patterns — ai/server.ts's live-extracted default masterSecret and FAKE api key, cell 6's dev masterSecret literal, and the sk-ant-api03 / sk-fake prefix families — failing on any hit. |
| `p3.face.test.tsx` | 145 | 3 tests: overlaid /analysis verdicts render paints equal to the canonical schema/gen worstOfVerdict derivation (pin-verified, worstOfCheck consistency incl. the ruling-8 lead-capped node), served nulls stay grey not-yet-computed, and an unrecognized worstOf token ranks worst and renders unknown. |
| `p3.face.honesty.test.tsx` | 171 | 7 tests + 1 declared todo: overlay id-safety (byte-identical ids, foreign-snapshot verdicts refused as analysis-graph-mismatch), fetchAnalysis honesty (typed pending refusals verbatim, analysis-shape-mismatch on drifted 200, hub-unreachable on transport death), and App-level error surfaces (named banner, pending analysis honesty); the live GET /analysis integration stays test.todo for run_demo.py. |
| `shell.face.test.tsx` | 143 | 6 tests on menubar semantics through the real App over the mocked hub: disabled-with-reason items, Re-analyze POST /analyze with the landed hub vocabulary, headless edit-menu reasons, arrow/Esc keyboard nav with focus return, probed blocked clicks, and the View menu's tool-window toggle + theme flip persisted under pgshell.layout.v2 / prefs. |
| `shell.face.tabs.test.tsx` | 93 | 3 tests: explorer tree lazy /fs/list loads (child fetch on first expand only, cached after), tab-switch dispose-before-mount ordering probed, and the dirty-close confirm guard keeping the tab on refusal. |
| `shell.face.save.test.tsx` | 92 | 4 tests: Ctrl+S PUTs the buffer and clears dirty on the hub ack (probed), a path-escape refusal surfaces its named class and keeps dirty, Ctrl+, prefs font-size change persists under pgshell.prefs.v1 (probed, paged Settings), and status-bar facts come from served /health + envelope + the joined bus. |
| `shell.face.dialogs.test.tsx` | 62 | 3 tests: Help>About carries OUT_OF_SCOPE_VERBATIM + the stub-floor tier statement, the root picker declares roots via POST /analyze{roots} (probed), and the gap report is disabled with the pending reason while /analysis is uncomputed. |
| `shell.units.test.ts` | 123 | 13 tests on pure chrome machinery: clampSplit min-size math (incl. degenerate container), prefs versioned persistence with probed changes and loud corrupt-blob resets, font-size clamp on load, recents dedupe/bound, and layout v1 save/load/reset with floored minimums. |
| `shell.units.tabs.test.ts` | 88 | 6 tests on TabsStore: probed activate (dispose+mount note), no duplicate tabs on re-open, dirty tracking against savedContent, markSaved sha recording, the probed unsaved-close guard, and clean-close neighbor activation. |
| `shell.units.fs.test.ts` | 111 | 8 tests: fsSource passes hub typed refusals through verbatim (path-escape, workspace-not-open, fs-io-error), names fs-shape-mismatch on off-contract 200s across all five fetchers, names hub-unreachable on transport death, parses happy shapes whole; plus fileIcon, spanFileMatches segment-suffix bound, pyright-canonical uri, languageIdFor. |
| `shell1c.face.test.tsx` | 111 | 3 tests: Close project returns to the welcome screen (probed, recents rendered, New-empty honest-disabled, Open-sample re-enters the hub flow with a probed refresh), Search Everywhere merges a bounded probed /fs/list walk with served graph nodes and selects over the V5 bus, and pure searchHits bounds rows at SEARCH_MAX_ROWS. |
| `shell1c.face.windows.test.tsx` | 147 | 6 tests: accent hex pref drives --acc only (probed, invalid drafts never persist), gear-menu view-mode transitions probed + persisted with the Window row honest-disabled by the verbatim README string, MOVE TO docks pinned, rail toggles mirror the View menu, Reset restores spec defaults, and breadcrumbs/hub-chip read served data. |
| `shell1c.layout2.test.ts` | 166 | 15 tests on pure layout2 geometry: spec defaults (windows + 296/340/204 edge sizes), even edge-stack splits with 10px gutters/6px margins, float clamps, undock overlay rects at z 44, z-band ordering, drop-zone hit bands (70/70/80 with corner precedence) + highlight rects, and dock insets by open docked edges. |
| `shell1c.layout2.machine.test.ts` | 93 | 6 tests on the WinState machine: every mode transition probed with from/to, MOVE TO docks pinned, monotonic float z-counter with no-op fronting, probed toggle/hide, auto-hide of exactly the open unpinned/undocked transients, and per-edge size clamps. |
| `shell1c.layout2.persist.test.ts` | 96 | 7 tests on layout2 persistence: v2-key round-trip (probed), loud corrupt-blob reset, load-time size clamps, loud probed v1->v2 migration keeping v1 for rollback with no re-migration, loud reset on an unparseable v1 blob, probed key removal on reset, and the five-window WIN_IDS contract. |
| `shell1c.skin.test.tsx` | 199 | 3 tests guarding the cell-5 DOM skin: app/src restates none of the five canonical verdict hexes, node fill/ring computed+inline paints are identical with the real stylesheet enabled vs disabled (sheet liveness proven via the skin-only box-sizing property) while inline paints match COLORS[data-fill-status], and no !important rides paint-bearing properties in styles.css. |
| `v4.serve.test.tsx` | 143 | 2 tests against its own spawned real python hub: /health schema-pin equality across hub, graphSource and view wall (plus catalog presence and the honestly-absent capability stream), and the three-way id-set/byte equality of served /graph, the model wall's pin-surface truth, and the view dump — with cross-boundary pin-vs-pin count reconciliation; file name kept because faultcheck fault (b) invokes it by name. |
| `v4.serve.verdicts.test.tsx` | 98 | 2 tests on its own spawned hub: unknown fills/null outlines cross extractor->model->hub->view uninvented (hatched grey, never the green hex, unknownGuard once per node), and leads render only as dashed leadEdge with hover degradation declared on link.hover.unsupported. |
| `v4.serve.guards.test.tsx` | 138 | 3 tests on its own spawned hub: the doctored edge+lead drop is caught by name (serializer-edge-drop) with pins proving which ids vanished and clean re-verification after, unknown-endpoint refusals pass through verbatim with the hub's own refusal pin, and both membranes independently refuse drifted/missing envelope pins with pins attached. |
| `v4.serve.trace.test.tsx` | 111 | 1 test on its own spawned hub: proves one Node.id UTF-8 byte-identical at extractor pin -> model-wall ingest -> served /graph bytes -> view dump, then writes and read-back-verifies vessels/TRACE-node.json for the Phase-3 trace. |
| `v5.bus.test.tsx` | 140 | 3 tests: both walls mount on one joined bus with identical carried schema pins (select-only subscription, hover-unbridged declared), editor caret select crosses byte-equal to the graph wall with honest headless centered:false, and a graph click reaches the editor's recv/reveal path with echo guards probed on both sides. |
| `v5.bus.bounds.test.tsx` | 125 | 3 tests: one select each direction produces exact bounded event counts on the bus and both pin streams (no echo storm), an unknown editor-side id probes link.select.unknownId with the join staying live, and the skeleton-envelope mount proves mutual-unknown no-crash in both directions. |
| `v5.bus.guards.test.tsx` | 105 | 3 tests: hover degrades probed (link.hover.unsupported, nothing reaches the editor), wrong-origin injections at either port are dropped and named bus-origin-spoof in the adapter log with each cell's own guard probing locally, and post-dispose clicks cross nothing with a second dispose idempotent. |
