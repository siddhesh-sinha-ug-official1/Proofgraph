# structure-extractor/extractor/docks

Per-language docks behind the `base.py` contract (decide_* constructors and the single validated edge builder every dock uses): python (grimp + pyright sub-paths, tier-gated at CT), lean (the live CT kernel-driver path plus the G placeholder path), and latex/typst design-stubs at G emitting leads only; `reasons.py` is the verbatim resolver-reason taxonomy.

## Files (verified)

Line counts and verified-purpose lines below are copied verbatim from the adversarial claim audit (`proofgraph/audit/AUDIT-structure-extractor.json`); each purpose line was written from the code itself and checked against the file's tests.

| File | Lines | Verified purpose |
|---|---:|---|
| `__init__.py` | 7 | Dock registry: re-exports the contract datatypes and maps lang -> dock class in ALL_DOCKS (python/lean/latex/typst). |
| `base.py` | 146 | The Dock contract: CandidateEdge/ResolverDecision/HonestCeiling/DockResult datatypes, the decide_* constructors, edges_from_decisions (the single validated edge builder every dock uses), and the innermost-owner/node-at span lookups. |
| `latex_dock.py` | 135 | LaTeX dock, design-stub at G: turns T1 labelref/cite/include anchors into references/cites/includes candidates, all emitted as U_STUB leads (dups X_DUP, ownerless X_NO_OWNER, both probed), probes a syntactic unused-label finding at its declared grade, and declares the LaTeXML/texlab designed pipeline in its ceiling. |
| `lean_common.py` | 56 | Lean dock shared bits: STAGE, driver dir/timeout constants, cell-2 pin string (divergence declaration only), lean-exe resolution ($LEAN_EXE -> elan shim -> PATH), driver pos->byte conversion, the typed DriverDead failure and the run_ref evidence helper. |
| `lean_ct.py` | 162 | CT orchestrator: probes lean facts and the toolchain-pin divergence, drives the driver once per file, matches driver decls to nodes via match_decls (every decision probed), applies verdicts, mints proof_uses/imports edges and dead-file leads, and declares the kernel-grade ceiling with the driver's limits[] verbatim. |
| `lean_ct_driver.py` | 89 | run_driver: spawns `lean --run Driver.lean <file>` with cwd pinned to lean_driver/ (toolchain rule), tree-kills on timeout (taskkill /T), types crash/bad-json failures as DriverDead, hashes verbatim stdout as run evidence, probes req/resp/timing. |
| `lean_ct_edges.py` | 194 | CT edge minting: proof_uses from decls[].refs filtered to owned+ingested decls (X_CORE/X_SELF/X_DUP rejections probed, never silent), imports from elaborated headers merged with T1 anchors (implicit Init = X_CORE; unelaborated anchor = lead), and U_BACKEND_* leads plus unknown-verdict probes for driver-dead files. |
| `lean_ct_fill.py` | 94 | apply_file_verdicts: maps driver verdicts onto one file's decl nodes via lean_verdict (fill+origin written only for non-unknown; green source cites decl + runSha), probes every verdict and unusedHypotheses payload, and surfaces errors outside every decl span as red on the module node. |
| `lean_dock.py` | 121 | LeanDock facade: dispatches CT tier -> extract_ct and every other tier -> extract_g (placeholder preserved; no self-upgrade), probes the Lean facts (import-cycle NA, #8840) on both paths, threads driver knobs, and re-exports lean_verdict/match_decls for tests. |
| `lean_g.py` | 128 | G-tier placeholder path: T1 import anchors and syntactic name co-occurrence become candidate seeds, every one a lead (U_TIER_G/U_STUB) or a probed rejection; declares the designed InfoTree/lake ceiling with design-stub status; byte-stable behavior pinned by the tier-G golden. |
| `lean_verdicts.py` | 133 | Pure functions: lean_verdict (green only kernelAccepted and not usesSorry and unexpectedAxioms=[] in an error-free file; sorry->amber; in-span error->red; else unknown — never green from absence) and match_decls (per-file exact-beats-suffix written-identifier matching where any tie on either side attests nothing). |
| `pyright_backend.py` | 33 | Facade re-exporting the pyright protocol, typed failures, position math and the three backend implementations from pyright_common/pyright_lsp/pyright_recorded; public surface unchanged since the SUB200 split. |
| `pyright_common.py` | 85 | Shared pyright seam: BackendTimeout/LspError/StaleRecordingError, the PyrightBackend protocol, byte-offset <-> LSP-position conversion (UTF-16 aware), and normcase path/uri helpers absorbing Windows drive-letter case. |
| `pyright_lsp.py` | 187 | Live pyright-langserver JSON-RPC client over an npx subprocess: didOpens absolute paths under the detected project root, maps response uris back into the dock's rel vocabulary (normcase reverse table), answers server config requests, tree-kills the cmd/npx process tree on close (Windows). |
| `pyright_recorded.py` | 74 | RecordedPyrightBackend replays a v2 recording only when every per-file sha matches the current sources (StaleRecordingError otherwise, pre-v2 refused); RecordingWrapper wraps a live backend and captures {files: shas, responses} for later replay. |
| `python_ceiling.py` | 57 | Python dock constants (STAGE, PY_BUILTINS, dynamic-import callees, designed resolves) and effective_ceiling — reports what THIS run could actually resolve given tier/packages/backend, with the full design in extra.designedResolves. |
| `python_dock.py` | 153 | PythonDock facade: tier-gates grimp and pyright at CT, probes no-package/loose-module caps, delegates the grimp and pyright sub-paths (shared duplicate-key set keeps resolved edges 1:1 through dedup), probes every decision + edge provenance, records decision D2, returns the effective ceiling. |
| `python_grimp.py` | 108 | grimp sub-path: builds the real import graph per package (sys.path pinned to the project root), probes modules/pairs, resolves module->module imports when both ends are ingested (typed rejections/leads otherwise), and probes grimp downstream-reachability answers for configured roots. |
| `python_pyright_path.py` | 174 | pyright sub-path: turns call/base anchors into calls/inherits decisions — owner lookup (X_NO_OWNER probed), dynamic-import and dynamic-callee leads, builtin rejection Pyright-first (name-heuristic only without a backend), typed timeout/LSP-error leads, X_SELF collapse and the C10 duplicate-collapse gate. |
| `reasons.py` | 66 | The verbatim resolver-reason taxonomy (resolved/unresolved/rejected constants; this round's additions marked) plus histogram(), which buckets decisions per dock by these exact strings. |
| `typst_dock.py` | 125 | Typst dock, design-stub at G: turns T1 ref/import anchors into references/imports candidates, all leads (dups/ownerless typed + probed), probes the typst-query deprecation (D4) and the pinMain warning, and declares the typst-ide designed pipeline in its ceiling. |
